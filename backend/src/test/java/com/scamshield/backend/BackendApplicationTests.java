package com.scamshield.backend;

import org.junit.jupiter.api.Test;

class BackendApplicationTests {

    @Test
    void contextLoads() {
        // Spring context load test is skipped in unit mode
        // Run integration tests against a live server with real env vars
    }
}
